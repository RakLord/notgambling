{
  "name": "notgambling",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git checkout gh-pages && git rm -rf . && cp -r dist/* . && rm -rf dist && git add . && git commit -m 'Deploy site to GH Pages' && git push origin gh-pages && git checkout main"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.11"
  }
}
